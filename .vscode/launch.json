{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Testbench",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/obj/Vnand_gate",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build"
        }
    ]
}