{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Testbench",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/Vdisplay_driver",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build"
        }
    ]
}